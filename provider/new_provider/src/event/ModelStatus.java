package event;

/**
 * Represents different status changes that can occur in the model.
 */
public enum ModelStatus {
  TURN_CHANGED,  // When the active player switches
  GAME_OVER     // When the game ends
} 